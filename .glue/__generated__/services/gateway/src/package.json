{
  "name": "gateway",
  "private": true,
  "workspaces": [
    "gateway",
    "packages/**"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --if-present --legacy-peer-deps",
    "build": "npm run build --workspaces --if-present --legacy-peer-deps",
    "dev": "npm run dev --workspace @project/gateway"
  }
}